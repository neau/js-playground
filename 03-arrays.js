// define an array and list its members
var bucketList = ['Rappel into a cave', 'take a falconry class', 'Learn to do a handstand'];

function listMembers() {
        for (var i = 0; i < (bucketList.length); i++) {
        console.log(bucketList[i]);
    }
}
listMembers();